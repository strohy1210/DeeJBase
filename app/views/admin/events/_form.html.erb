<%= form_for(@event, url: admin_event_path(@event),:html => { :multipart => true }, method: "patch") do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <strong>Venue:</strong>
  <%= @event.venue.name if @event.venue %>
</p>

<p>
  <strong>Dj:</strong>
  <%= @event.dj.name if @event.dj %>
</p>

<p>
  <strong>Promoter:</strong>
  <%= @event.promoter.name if @event.promoter %>
</p>


  <div class="field">
    <%= f.label :venue_id %><br>
    <%= f.number_field :venue_id %>
  </div>
  <div class="field">
    <%= f.label :dj_id %><br>
    <%= f.number_field :dj_id %>
  </div>
  <div class="field">
    <%= f.label :promoter_id %><br>
    <%= f.number_field :promoter_id %>
  </div>
  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
